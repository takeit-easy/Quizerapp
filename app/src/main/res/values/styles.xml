<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="OfferItemStyle">
        <item name="android:padding">@dimen/standart_padding_table</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:textAllCaps">false</item>
        <item name="android:padding">@dimen/standart_padding</item>
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:layout_margin">@dimen/standart_margin</item>
    </style>

    <style name="Header">
        <item name="android:textSize">18sp</item>
        <item name="android:paddingTop">@dimen/standart_padding</item>
        <item name="android:paddingBottom">@dimen/standart_padding</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
